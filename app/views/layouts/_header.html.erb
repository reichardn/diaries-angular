<nav class="">
    <ul>
        <li> <%= link_to "Home", root_path %> </li>
        <% if user_signed_in? %>
          <% if !!current_diary %>
            <li><%= link_to "Current Diary", diary_path(current_diary) %></li>
          <% else %>
            <li><%= link_to "Create Current Diary", diaries_path, method: 'post' %></li>
          <% end %>
          <li> <%= link_to "All Diaries", user_diaries_path(current_user) %></li>
          <li><%= link_to 'Sign Out', destroy_user_session_path, method: 'delete' %></li>
        <% else %>
          <li><%= link_to 'Sign In', new_user_session_path %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
    </ul>
</nav>